# Prerequisite
* Install `nodejs` and `yarn`

# Run the project
```
yarn
yarn start
```

# Build the project
```
yarn 
yarn build
```

# Check the result

open browser: [http://localhost:3000](http://localhost:3000)